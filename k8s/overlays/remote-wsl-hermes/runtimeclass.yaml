# gVisor for the agent pods. The node needs the runsc handler registered in
# containerd; see docs/operations/hermes.md "gVisor on k3d/WSL2".
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
